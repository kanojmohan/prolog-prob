// Find the last element of a list.

// const colors = ["red", "yellow", "green", "blue", "orange"];
// const lastItem = colors[colors.length - 1];
// console.log(lastItem);

var group = ["a", "b", "c", "d"];

function lastElement(input) {
  let item;
  for (item of input) {
  }
  return item;
}
console.log(lastElement(group));
