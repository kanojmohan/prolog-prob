var group = ["a", "b", "c", "d"];
// Find the last but one element of a list.

function secondlElement(input) {
  let item;
  for (item in input) {
  }
  return input[item - 1];
}
console.log(secondlElement(group));
